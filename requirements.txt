# Requirements file with known vulnerable packages for testing pip_audit
# These packages have documented CVEs and security vulnerabilities

# Flask with multiple CVEs
Flask==0.12.0
Jinja2==2.10.0

# Requests with known vulnerabilities
requests==2.20.0
urllib3==1.24.0

# Pillow with security vulnerabilities
Pillow==5.4.0

# Django with multiple CVEs
django==2.2.0

# NumPy with vulnerabilities
numpy==1.16.0

# Other vulnerable packages
SQLAlchemy==1.2.0
PyYAML==3.13

# Safe packages (for comparison)
certifi==2019.11.28

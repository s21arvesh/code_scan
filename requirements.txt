# Requirements file with vulnerable packages for testing pip_audit
# These packages have known security vulnerabilities

# Flask with known vulnerabilities
Flask==1.0.0
Jinja2==2.11.0

# Requests with known vulnerabilities  
requests==2.25.0
urllib3==1.26.0

# Other vulnerable packages
pillow==8.0.0
django==3.1.0
numpy==1.19.0

# Celery and other packages
celery==5.3.4
PyYAML==6.0.3
base64==1.0.0
